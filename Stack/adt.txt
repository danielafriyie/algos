Stack:
    push(element): adds element to the top of the stack
    pop(): removes and returns the top element from the stack or null if empty
    top(): returns the top element of the stack or null
    size(): returns the number of elements in the stack
    isEmpty(): returns boolean indicating whether the stack is empty